{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Windows launch",
            "program": "${workspaceRoot}/main.exe",
            "type": "cppvsdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}